# ── SpotCloud — variables d'environnement frontend ──
# En production (Vercel), URL du backend Go déployé sur Render :
NEXT_PUBLIC_API_BASE=https://votre-service.onrender.com

# En local sans cette variable, l'app utilise le mode passerelle
# (fetch('/api/...?XTransformPort=4000')) — voir src/lib/hotspot/api.ts.
