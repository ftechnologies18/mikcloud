import LoginRoute from "@/components/hotspot/routes/login-route";

// /login — écran de connexion (deep-linkable, back navigateur fonctionnel).
export default function LoginPage() {
  return <LoginRoute />;
}
